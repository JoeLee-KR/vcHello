# --- Dependencies (의존성 라이브러리) ---
vendor/
__pycache__
.venv
node_modules/
package-lock.json # 개인 취향에 따라 커밋하기도 하지만, 혼자 테스트용이라면 제외하기도 함 (보통은 커밋 권장)
yarn.lock
pnpm-lock.yaml

# --- Environment Variables (보안 필수: API 키 유출 방지) ---
# AI 툴들은 API Key를 .env 파일에 저장하는 경우가 많습니다. 절대 커밋하면 안 됩니다.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env
*.key

# --- Logs (로그 파일) ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# --- Build Outputs (빌드 결과물) ---
dist/
build/
out/
target/

# --- IDE & Editor (에디터 설정 파일) ---
.vscode/
.idea/
*.swp
*.swo

# --- OS System Files (운영체제 파일) ---
.DS_Store # macOS
Thumbs.db # Windows
ehthumbs.db # Windows
.Spotlight-V100 # macOS
.Trashes # macOS

# --- Vibe Coding specific (AI 도구 관련 임시 파일) ---
# 도구에 따라 대화 기록이나 임시 파일을 생성할 수 있습니다.
# (필요시 추가)
